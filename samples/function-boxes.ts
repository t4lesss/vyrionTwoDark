// function em comentários ou strings conserva seu estilo.
const example = "function help() {}";

function help(): string {
    return "Olá, Tales!";
}

async function lookup(name: string): Promise<string> {
    return name;
}

const arrow = (value: string) => value;
